# Browser-Key-Extraction
Browser key extraction is the process of locating and obtaining the cryptographic keys or keying material a browser uses to encrypt secrets (saved passwords, cookies, tokens). An attacker who extracts those keys can often decrypt the browser’s stored credentials and other sensitive data.
